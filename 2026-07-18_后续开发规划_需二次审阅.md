# ESP32-P4 AI 宠物后续开发规划（需二次审阅）

> 日期：2026-07-18  
> 状态：**方案草案，需硬件、嵌入式、结构、电源联合二次审阅**  
> 目标工程：`xiaozhi-esp32` / `esp32-p4-ai-pet`  
> 目标板卡：Waveshare ESP32-P4-WIFI6-Touch-LCD-7B 改造样机  
> 基线 ESP-IDF：本地 `v5.5.2`  
> 本文依据：当前源码、2026-07-18 构建产物、ESP-IDF 5.5 ESP32-P4 正式文档、ESP32-P4 数据手册

---

## 1. 文档目的与结论摘要

当前工程已经完成小智语音基线、AI Pet 专用板型、单眼 GC9A01 点亮、眼睛整帧资源、视线/眨眼/闭眼和 MCP 语音控制。后续开发重点不再是证明单个外设可以工作，而是建立可持续扩展的存储布局、内存策略和统一宠物行为编排层。

本轮结论如下：

1. **当前最紧张的是 4 MiB OTA 应用槽，不是 PSRAM。** `xiaozhi.bin` 已使用 92.53%，只剩 313,232 B。
2. 9 张眼睛 RGB565 整帧共 1,036,800 B，约占当前应用镜像 26.71%；应优先迁入 16 MiB `assets` 分区。
3. ESP32-P4 的 Flash 与 PSRAM 使用两条独立 SPI 总线；当前工程还启用了 `CONFIG_SPIRAM_XIP_FROM_PSRAM=y`。ESP-IDF 明确说明，在这种配置下 SPI1 Flash API 不走传统的“关闭 cache、暂停其他任务”路径，不能沿用 ESP32/S3 的结论简单判定 PSRAM 在 Flash 操作时不可访问。
4. XIP 并非没有代价：应用 `.text` 和 `.rodata` 会映射/加载到 PSRAM，代码执行、眼睛 framebuffer、摄像头缓冲和其他大块数据将共同竞争 PSRAM 总线。是否长期保留 XIP 必须通过 A/B 压测决定。
5. FreeRTOS 普通 `xTaskCreate()` / `xTaskCreatePinnedToCore()` 默认仍把任务栈放在内部 RAM。当前不启用“所有任务栈自动放 PSRAM”，只保留按任务显式选择的能力。
6. 后续应新增 `PetBehaviorController`，统一驱动双眼、双舵机和 WS2812，禁止三套外设各自重复读取 `Application` 状态并独立做决策。
7. 开发顺序建议为：**资源迁移与监控 → 双眼 → 行为控制器 → WS2812/舵机 → K230 UART 视觉 → 眼头协同 → 稳定性与产品化**。

---

## 2. 资料依据与适用边界

### 2.1 ESP-IDF / 芯片正式资料

- ESP-IDF 5.5，ESP32-P4 SPI Flash 并发约束：  
  <https://docs.espressif.com/projects/esp-idf/en/v5.5/esp32p4/api-reference/peripherals/spi_flash/spi_flash_concurrency.html>
- ESP-IDF 5.5，ESP32-P4 Heap Memory Allocation：  
  <https://docs.espressif.com/projects/esp-idf/en/v5.5/esp32p4/api-reference/system/mem_alloc.html>
- ESP-IDF 5.5，ESP32-P4 FreeRTOS：  
  <https://docs.espressif.com/projects/esp-idf/en/v5.5/esp32p4/api-reference/system/freertos.html>
- ESP-IDF 5.5，ESP32-P4 Memory Types：  
  <https://docs.espressif.com/projects/esp-idf/en/release-v5.5/esp32p4/api-guides/memory-types.html>
- ESP-IDF 5.5，ESP32-P4 RMT：  
  <https://docs.espressif.com/projects/esp-idf/en/v5.5/esp32p4/api-reference/peripherals/rmt.html>
- ESP-IDF 5.5，ESP32-P4 LEDC：  
  <https://docs.espressif.com/projects/esp-idf/en/v5.5/esp32p4/api-reference/peripherals/ledc.html>
- ESP32-P4 Series Datasheet：  
  <https://documentation.espressif.com/esp32-p4_datasheet_en.pdf>

### 2.2 当前工程证据

- `build/xiaozhi.bin`：2026-07-18 构建，3,881,072 B。
- `build/xiaozhi.elf`、`build/xiaozhi.map`：用于内部 SRAM、IRAM、DRAM 静态统计。
- `build/generated_assets.bin`：2,851,767 B。
- `partitions/v2/32m.csv`：双 4 MiB OTA + 16 MiB assets。
- `sdkconfig`：AI Pet 板型、32 MB Flash、PSRAM 200 MHz、XIP from PSRAM。
- `main/boards/waveshare/esp32-p4-ai-pet/`：当前单眼及 MCP 实现。

### 2.3 边界说明

- 本文中的 Flash/内部 SRAM 静态占用来自真实构建产物。
- 当前没有保存完整的运行时 heap/PSRAM 日志，所以“启动后剩余量、峰值和碎片”仍需上板采集。
- ESP32-P4 数据手册给出 16 MB 和 32 MB 封装内 PSRAM型号；本样机实际 PSRAM容量必须通过启动日志或 `heap_caps_get_total_size(MALLOC_CAP_SPIRAM)`确认，不能仅凭板名推断。
- GPIO、电源、舵机机械范围和第二眼装配方向需要二次硬件审阅。

---

## 3. 当前资源基线

### 3.1 Flash / OTA 应用槽

| 项目 | 当前值 | 判断 |
|---|---:|---|
| 物理 Flash 配置 | 32 MiB | 已配置 |
| 单个 OTA 应用槽 | 4,194,304 B | 固件硬上限 |
| `xiaozhi.bin` | 3,881,072 B | 92.53% |
| OTA 槽剩余 | 313,232 B | 仅约 305.9 KiB，风险高 |
| `generated_assets.bin` | 2,851,767 B | assets 分区使用约 17% |
| 9 张眼睛整帧 | 1,036,800 B | 当前嵌入应用镜像 |

当前 Flash 总容量并不等于应用可用容量。OTA 更新要求新固件完整放入另一个 4 MiB 应用槽，后续功能必须以 4 MiB 为限制进行预算。

若迁出 9 张眼睛整帧，应用镜像理论上可下降至约 2,844,272 B，4 MiB OTA 槽使用率约 67.8%。实际结果需以迁移后的构建报告为准。

由于当前同时启用了 `CONFIG_SPIRAM_RODATA` 和 `CONFIG_SPIRAM_XIP_FROM_PSRAM`，嵌入应用 `.rodata` 的眼睛帧还可能进入运行时 PSRAM映射/加载范围。因此迁移眼睛资源的收益不仅是释放 OTA槽，也可能降低启动后的 PSRAM占用和 PSRAM总线压力；具体释放量必须通过迁移前后的运行时统计确认。

### 3.2 内部 SRAM 静态占用

| 区域 | 当前值 | 判断 |
|---|---:|---|
| 低地址 SRAM 容量 | 183,248 B | IRAM/DRAM 共用 |
| 低地址静态占用 | 152,736 B | 83.35% |
| 低地址链接后剩余 | 30,512 B | 静态余量偏紧 |
| 高地址内部 SRAM | 393,216 B | 主要进入运行时内部堆 |
| 链接完成后理论内部堆上限 | 约 423,728 B | 未扣除启动动态分配 |
| 单眼 framebuffer | 115,200 B | 当前优先分配到 PSRAM |
| `pet_eye` 任务栈 | 4,096 B | 普通任务创建，默认内部 RAM |

内部 SRAM 后续主要留给：

- FreeRTOS 任务栈和 TCB；
- ISR/cache-safe 数据；
- 驱动控制块、队列、互斥锁；
- 对延迟敏感的小型状态；
- 不支持外部 RAM 的 DMA 描述符或缓冲；
- Flash/NVS/OTA 等明确要求内部内存的工作缓冲。

### 3.3 当前 PSRAM / XIP 配置

当前 `sdkconfig` 中与本方案直接相关的配置：

```text
CONFIG_SOC_MEMSPI_FLASH_PSRAM_INDEPENDENT=y
CONFIG_SPIRAM_SPEED_200M=y
CONFIG_SPIRAM_FETCH_INSTRUCTIONS=y
CONFIG_SPIRAM_RODATA=y
CONFIG_SPIRAM_XIP_FROM_PSRAM=y
CONFIG_SPIRAM_FLASH_LOAD_TO_PSRAM=y
CONFIG_SPIRAM_MALLOC_RESERVE_INTERNAL=65536
CONFIG_FREERTOS_TASK_CREATE_ALLOW_EXT_MEM=y
# CONFIG_SPI_FLASH_AUTO_SUSPEND is not set
```

ESP-IDF 对 ESP32-P4 的正式说明是：

- P4 的 Flash 和 PSRAM 使用两条独立 SPI 总线；
- 启用 `CONFIG_SPIRAM_XIP_FROM_PSRAM` 后，Flash `.text` 和 `.rodata` 放入/映射到 PSRAM；
- ESP-IDF SPI1 Flash API在此配置下不会按传统路径关闭 cache；
- Flash 操作期间，PSRAM cache 可以继续读写；
- 代价是固件代码、只读数据与应用 PSRAM缓冲共享 PSRAM总线，需要做性能分析。

因此，本项目后续不采用“PSRAM 一定不能和 Flash 同时工作”的前提，也不采用“P4 上 PSRAM 完全没有限制”的相反前提，而是以运行压测决定具体策略。

---

## 4. 内存与资源管理策略

### 4.1 总原则

1. **资源文件优先进入 assets 分区，不长期嵌入 OTA 应用。**
2. **大块、可缓存、非 ISR 控制数据优先使用 PSRAM。**
3. **小块、高频、实时、控制类数据优先使用内部 RAM。**
4. **任务栈默认使用内部 RAM，不启用全局自动外部栈。**
5. **PSRAM 分配失败不得无条件吞噬内部 RAM。** 大 framebuffer应清晰失败或降级，不能静默回退后挤垮音频/网络。
6. 所有大块分配必须记录用途、大小、capability、失败策略和释放时机。

### 4.2 建议放入 PSRAM 的对象

- 单眼/双眼工作 framebuffer；
- 眼睛资源解码或缩放临时缓冲；
- 摄像头帧、JPEG编码临时缓冲；
- K230 UART较大的历史帧/调试抓包环形缓冲；
- 大于约 8 KiB 且不在 ISR/cache-safe 路径访问的数组；
- 可重建缓存、日志批量缓存和低优先级数据。

推荐显式使用：

```cpp
heap_caps_malloc(size, MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT);
```

涉及 P4 EDMA 时，按具体外设要求使用 `MALLOC_CAP_SPIRAM | MALLOC_CAP_DMA` 和对齐分配，不能假设所有驱动都接受任意 PSRAM地址。

### 4.3 必须或优先保留在内部 RAM 的对象

- ISR直接访问的数据；
- cache-safe 中断回调及其所有调用链数据；
- FreeRTOS队列、互斥锁、事件组等核心控制对象；
- 默认任务栈和 TCB；
- 舵机控制状态、限位、故障标志；
- RMT/LEDC驱动控制结构和对实时性敏感的小缓冲；
- Flash/NVS/OTA路径上的内部工作缓冲；
- 音频实时路径中对确定性有要求的小块数据。

### 4.4 任务栈策略

ESP-IDF 5.5 的默认行为是：普通 `xTaskCreate()` 和 `xTaskCreatePinnedToCore()` 的动态内存由 FreeRTOS放在内部 RAM。只有显式使用 `...CreateWithCaps()`，或用外部 RAM缓冲调用 `xTaskCreateStatic()`，才会把任务栈放入 PSRAM。

本项目建议：

- 不改变 ESP-IDF 5.5.2 的默认任务创建策略；普通动态任务栈继续使用内部 RAM；
- 新增的 `PetBehaviorController`、舵机、WS2812、K230 UART任务均使用内部任务栈；
- `CONFIG_FREERTOS_TASK_CREATE_ALLOW_EXT_MEM` 暂时保留，因为现有唤醒词编码路径显式使用 PSRAM任务栈；
- 对现有两处唤醒词编码任务建立专项压测，不在未验证前直接修改上游音频逻辑；
- 若 OTA/NVS/Flash压力测试发现异常，再把这两处任务栈迁回内部 RAM，并评估关闭允许外部栈的配置；
- 每个新增任务必须采集 `uxTaskGetStackHighWaterMark()`，以实际高水位收敛栈大小。

### 4.5 是否保留 XIP from PSRAM

当前不能仅根据“PSRAM较大”或“Flash期间可运行”决定长期保留 XIP。需要构建两个配置进行 A/B 对比：

| 测试项 | XIP开启 | XIP关闭 |
|---|---|---|
| 启动后 PSRAM总量/剩余量 | 记录 | 记录 |
| 内部 IRAM/DRAM静态占用 | 记录 | 记录 |
| 单眼/双眼稳定帧率 | 记录 | 记录 |
| 音频播放丢帧/卡顿 | 记录 | 记录 |
| 摄像头拍照耗时 | 记录 | 记录 |
| OTA/NVS写入期间表现 | 记录 | 记录 |
| PSRAM带宽压力下 CPU负载 | 记录 | 记录 |
| 30分钟最小剩余 heap | 记录 | 记录 |

决策标准：优先保证语音和实时控制稳定，其次才是眼睛帧率。若 XIP导致 PSRAM总线竞争明显，应评估关闭 XIP并仅把真正要求 Flash 操作期间继续运行的代码放入 IRAM/DRAM。

---

## 5. 双眼显示后续方案

### 5.1 硬件与驱动结构

- 左右眼共用 SPI2 的 SCLK、MOSI、DC、RST；
- 左眼保留 CS=IO5；
- 右眼暂定 CS=IO29，必须经硬件二审确认；
- 建立两个 `esp_lcd_panel_handle_t`，不要建立两套互不协调的动画任务；
- 第一版共享一个 115,200 B 工作 framebuffer；
- 左右眼需要不同画面时，在同一个 worker 中串行渲染/刷新；
- 加入单眼缺失或初始化失败的降级路径。

### 5.2 带宽预算

当前刷新率 `kFps=12`：

```text
单眼：115,200 × 12 = 1.382 MB/s
双眼：115,200 × 12 × 2 = 2.765 MB/s
SPI 40 MHz理论上限：约 5 MB/s
```

双眼纯像素数据已占理论总线约 55%，还未计入命令、CS切换、驱动和 cache开销。后续应改为事件驱动刷新：

- 静态状态只在内容变化时刷新；
- 眨眼或过渡期间临时提高刷新率；
- 空闲稳定帧不持续重复全屏传输；
- 必要时做区域刷新或降低动态阶段帧率；
- 眼睛源资源只保留一份，左右眼通过偏移/镜像生成。

### 5.3 资源迁移

第一优先级把 9 张 RGB565帧从 `main/CMakeLists.txt` 的 `EMBED_FILES` 迁至 `assets` 分区。推荐二审比较两种实现：

1. 复用现有 assets打包/索引机制；
2. 新增独立只读 eye-assets分区并通过 mmap读取。

选择标准：OTA兼容、资源可独立升级、读取延迟、实现复杂度和失败回退。

---

## 6. 统一宠物行为编排方案

### 6.1 架构目标

新增 `PetBehaviorController`，把系统状态、用户命令和视觉事件转换成统一行为意图：

```text
DeviceState ─┐
MCP命令 ─────┤
K230视觉 ────┼─> PetBehaviorController ─> EyeIntent   ─> 双眼
空闲定时器 ──┤                         ├> HeadIntent  ─> 双舵机
错误/休眠 ───┘                         └> LedIntent   ─> WS2812
```

`Application` 只发布状态，不直接编排宠物动作；眼睛、舵机和灯带驱动也不各自实现一套业务状态机。

建议的统一输出：

```cpp
struct PetBehaviorFrame {
    EyeEmotion eye_emotion;
    float gaze_x;
    float gaze_y;
    bool blink;
    bool eyes_closed;

    float head_pan_deg;
    float head_tilt_deg;
    float head_speed_deg_s;

    LedEffect led_effect;
    RgbColor led_color;
    uint8_t led_brightness;

    int priority;
    uint32_t duration_ms;
};
```

### 6.2 仲裁优先级

```text
100 机械保护、Fatal Error、固件升级
90  睡眠/闭眼锁定
80  用户明确 MCP 动作
60  Listening / Speaking 对话状态
50  K230 人脸跟踪和表情事件
10  自动空闲行为
```

每个临时行为必须带超时；睡眠、机械故障等锁定行为必须显式解除。禁止“最后一次调用永久覆盖全部自动行为”的隐式状态。

### 6.3 自动映射表

| 状态 | 眼睛 | 舵机 | WS2812 |
|---|---|---|---|
| Starting | 中性、缓慢扫描 | 回中并保持 | 蓝白扫描 |
| Wi-Fi配置 | 搜索视线 | 默认不运动或极小幅观察 | 蓝色闪烁 |
| Connecting/Activating | 注视前方、扫描高光 | 回中 | 蓝紫呼吸 |
| Idle | 中性、自然眨眼、低频漂移 | 偶尔轻微歪头 | 低亮呼吸或关闭 |
| Listening | 睁大、注视用户 | 朝人脸缓慢跟随 | 青色活动效果 |
| Thinking（宠物层附加态） | 视线上看/右上 | 小幅歪头 | 紫色旋转 |
| Speaking | 当前情绪、自然眨眼 | 低频小幅点头 | 暖色随语音脉冲 |
| Upgrading | 固定等待表情 | 停止并回安全位 | 绿色快速闪烁 |
| Fatal/Error | 警觉或错误帧 | 停止动作 | 红色闪烁 |
| Sleep | 闭眼 | 进入休眠位后停止PWM | 渐灭 |

---

## 7. 舵机转头方案

### 7.1 驱动

- 首选 ESP-IDF LEDC，两路通道共用 50 Hz定时器；
- ESP32-P4 LEDC仅支持 low-speed mode，配置变化需软件显式触发；
- 二审确认时钟源、分辨率和轻睡眠行为；
- 舵机脉宽必须按实际型号和结构标定，不能直接假定 500～2500 μs全部安全；
- 提供中心、最小、最大、速度和加速度软限制；
- 所有命令在单一 `ServoController` worker 中串行执行，避免不同任务同时修改同一LEDC通道。

### 7.2 眼睛快速、头部慢速的级联跟踪

K230输出归一化人脸中心 `x/y ∈ [-1, 1]`：

1. 眼睛立即在 ±26 px范围内快速跟随；
2. 人脸偏离死区并连续稳定约 300 ms 后，头部开始慢速转向；
3. 头部接近目标后，瞳孔逐渐回到眼眶中心；
4. 人脸短暂丢失先保持，超过约 2 s 再缓慢回中；
5. 输出使用 EMA/限速/限加速度，避免检测噪声导致抖动。

建议初始参数仅作为调试起点：

```text
眼睛死区：|x|、|y| < 0.10
舵机启动死区：|x|、|y| > 0.20～0.25
持续判定：200～400 ms
最大角速度：30～60°/s
丢脸保持：0.5～1.0 s
自动回中：丢脸约 2 s后
```

### 7.3 电气安全

- 舵机使用独立 5～6 V电源；
- P4 GPIO只输出控制信号；
- 舵机电源与P4必须共地；
- 增加保险、限流和足够的储能/去耦；
- 测试堵转峰值电流、复位、电源跌落、音频噪声和Wi-Fi稳定性；
- Listening期间尽量停止或降低舵机动作，减少机械噪声进入麦克风。

---

## 8. WS2812 灯带方案

### 8.1 驱动路径

- 复用工程现有 `CircularStrip` 和 `led_strip` RMT路径；
- ESP-IDF正式示例使用 RMT驱动 WS2812；
- 第一版由状态/情绪控制颜色、亮度、呼吸、闪烁和扫描；
- 不为灯带单独建立一套业务状态机；
- RMT编码回调保持短小，不在回调中日志打印或执行复杂逻辑。

### 8.2 与眼睛、舵机的关系

- 架构相同：均接受 `PetBehaviorController` 输出；
- 表达维度不同：眼睛表达表情/注视，舵机表达朝向/姿态，灯带表达系统状态/氛围；
- 第一版灯带不跟随人脸坐标，仅映射状态和情绪；
- 后续若使用环形灯，可再评估“人脸所在方向对应区域变亮”。

### 8.3 电源和亮度

- 灯带建议独立 5 V供电并与P4共地；
- 根据灯珠数量计算最坏电流，不按平均动画电流选电源；
- 设置软件总亮度上限；
- 入口增加适当电阻、储能电容；
- 3.3 V数据电平是否需要电平转换由灯带型号、线长和实测决定。

---

## 9. K230 主动视觉接入

P4不承担第一阶段人脸模型推理。K230输出结构化结果，P4负责行为闭环：

```json
{
  "face": true,
  "cx": 0.32,
  "cy": -0.15,
  "yaw": 18.2,
  "pitch": -7.5,
  "emotion": "happy",
  "confidence": 0.91
}
```

开发顺序：

1. UART供电、共地、波特率、握手和心跳；
2. 帧头、长度、序号、CRC、超时和重同步；
3. 人脸存在与中心点；
4. yaw/pitch和靠近/远离；
5. 表情类别和置信度；
6. 视觉事件门控；
7. 必要时触发小智单张照片解释或主动回应。

K230 RX任务使用内部任务栈；较大的接收历史/调试缓冲可放 PSRAM。实时解析队列保持短小，避免UART积压导致控制延迟。

---

## 10. 分阶段实施计划

### 阶段 A：资源和可观测性（P0）

- 迁移眼睛整帧到 assets或独立只读分区；
- 恢复 `idf.py size` / `size-components` 工具环境；
- 启动和周期打印内部 SRAM、PSRAM、最大连续块、历史最低值；
- 记录所有自建任务的 stack high-water mark；
- 建立固件体积门禁：OTA应用槽使用率目标低于 80%。

验收：OTA应用镜像获得至少约 1 MiB稳定余量，语音基线无回归。

### 阶段 B：双眼（P0）

- 双 panel、共享SPI、共享 framebuffer；
- 左右同步、镜像、独立视线偏移；
- 事件驱动刷新；
- 双眼+语音+Wi-Fi 30分钟测试。

验收：双眼稳定显示，音频无明显卡顿，PSRAM/内部heap无持续下降。

### 阶段 C：统一行为控制器（P0）

- 新建 `main/pet/pet_behavior_controller.*`；
- 接入 DeviceState listener；
- 把现有眼睛 MCP 改为发布行为意图；
- 实现优先级、TTL、锁定和回退；
- 眼睛先完成全状态映射。

验收：状态转换一致，MCP临时动作结束后能正确回到自动状态。

### 阶段 D：WS2812 + 双舵机（P1）

- RMT灯带驱动和亮度限制；
- 两路LEDC 50 Hz舵机；
- 机械标定、软限位、限速、回中和故障停机；
- 接入统一行为控制器。

验收：外设并发不破坏音频、Wi-Fi和眼睛SPI。

### 阶段 E：K230视觉闭环（P1）

- UART协议、心跳、CRC和掉线恢复；
- 人脸位置平滑；
- 眼睛快速跟随；
- 舵机慢速跟随；
- 表情到眼睛/灯带映射。

验收：连续视觉输入30分钟无丢帧失控；人脸丢失后安全回中。

### 阶段 F：体验与稳定性（P2）

- 自然眨眼、好奇、困倦、说话小动作；
- XIP on/off A/B测试；
- OTA/NVS写入并发压力；
- 电源、温升、舵机堵转和低压复位；
- 2小时以上综合老化。

验收：形成可冻结的硬件配置、资源预算和发布配置。

---

## 11. 测试与资源红线

### 11.1 建议红线

| 指标 | 建议目标 |
|---|---:|
| 单 OTA应用槽使用率 | < 80% |
| 内部 heap历史最低剩余 | 初版目标 > 80～100 KiB，二审后确认 |
| 内部 heap最大连续块 | 初版目标 > 32～64 KiB，二审后确认 |
| PSRAM分配失败 | 不允许静默回退吞噬大块内部 RAM |
| 自建任务栈水位 | 保留实际峰值之外至少 25%安全余量 |
| 双眼稳定测试 | ≥ 30 min |
| K230 UART稳定测试 | ≥ 30 min |
| 综合老化 | ≥ 2 h |

### 11.2 必须覆盖的并发场景

- 双眼刷新 + MQTT语音对话；
- 双眼刷新 + 摄像头单张拍照；
- 双眼 + WS2812 + 舵机动作；
- 语音播放 + 舵机小幅点头；
- OTA/NVS写入 + 音频/眼睛/灯带；
- K230持续UART上报 + 语音 + 双眼；
- K230掉线、CRC错误、重复帧和乱序恢复；
- PSRAM高占用、碎片化和分配失败降级。

---

## 12. 二次审阅清单

以下事项在实现前必须明确：

### 12.1 嵌入式审阅

- [ ] 实机PSRAM容量及启动后可用量；
- [ ] XIP on/off性能和资源差异；
- [ ] 眼睛资源进入现有 assets还是独立分区；
- [ ] 双眼刷新策略与目标帧率；
- [ ] 现有唤醒词PSRAM任务栈在OTA/NVS期间是否稳定；
- [ ] 内部heap和任务栈红线最终值；
- [ ] K230 UART GPIO、波特率和协议版本。

### 12.2 硬件/结构审阅

- [ ] 右眼CS=IO29是否最终可用；
- [ ] 两路舵机GPIO和机械方向；
- [ ] WS2812 GPIO、灯珠数和布线；
- [ ] K230独立电源和UART共地；
- [ ] 舵机转角软限位对应结构极限；
- [ ] 眼睛左右镜像、安装角度和排线长度。

### 12.3 电源审阅

- [ ] 舵机堵转峰值电流；
- [ ] WS2812全白最坏电流；
- [ ] K230峰值功耗；
- [ ] 电源分配、保险、储能和地线回流；
- [ ] 舵机/灯带动作对麦克风、Codec、C6 Wi-Fi和P4复位的影响。

---

## 13. 本轮建议冻结项

在二次审阅前，建议只冻结以下原则，不提前冻结具体GPIO和参数：

1. 眼睛资源迁出 OTA应用镜像；
2. 双眼共用 SPI、独立CS、共享工作缓冲；
3. 普通任务栈默认内部 RAM；
4. 大型非实时数据优先显式分配到 PSRAM；
5. XIP from PSRAM是否长期保留由 A/B压测决定；
6. 双眼、舵机、WS2812统一由 `PetBehaviorController` 编排；
7. P4负责实时行为控制，K230负责主动视觉推理，小智保留语音和单张照片解释路径；
8. 舵机、灯带、K230均使用独立合规供电，不由P4板载3.3 V轨直接承担。

二次审阅通过后，再把本文转为正式开发任务拆分、GPIO冻结表、接口协议和测试用例。
