# S3 大板人脸移动自动跟随方案

> 状态：设计稿，未烧录验收。  
> 硬件：09-13 原理图仅 **CN2 一路舵机**；型号 **MG90S**。  
> 原则：K230 全量推帧，ESP32 用**脸的位移**本地驱动；不走 MCP 闭环，不把 JSON 透传到云。

## 1. 目标

人左右走动或转头时，脖子（舵机）跟着转；眼睛同时看向位移方向。  
脸停住时舵机也停，**不要**为了把脸拉回画面正中而持续转动。

这和“位置回中”不同：

| 方式 | 输入 | 行为 | 本方案 |
|------|------|------|--------|
| 位置回中 | 当前 `dx`（脸离画面中心多远） | 一直转到让脸居中 | 不用 |
| **位移跟随** | 本帧相对上帧的 `Δx` | 脸怎么移，头跟多少；停则停 | **采用** |

## 2. 硬件边界

原理图 CN2（PH 3P）：

| 脚 | 网 | 作用 |
|----|----|------|
| 1 | GND | 地 |
| 2 | `SE_PWMO` → U3.12 → **GPIO8** | PWM 信号 |
| 3 | 5V 经 D5 | MG90S 电源 |

- 只有 **偏航（左右）** 一路。没有第二路 PWM，俯仰只做眼睛，不转舵机。
- MG90S：50Hz，脉宽约 0.5–2.5ms → 0–180°；跟随限幅建议中位 **90° ± 45°**。
- 背光已占用 LEDC T0/CH0，舵机用 **T1/CH1**。
- USB 5V 只够轻载；堵转约 0.7A，安装后先慢速、小行程。

K230 走 UART1（GPIO10/11），已在真机推 JSON。识别留在 K230，执行留在 S3。

## 3. 用哪路视觉量判断“移动”

K230 真机字段（已见过）：

```json
"face": {"present": true, "count": 1, "box": {"x": 13, "y": 0, "w": 210, "h": 182}},
"tracking": {"id": 19, "cx": 128.2, "cy": 87.6, "dx": -0.599, "dy": -0.635, "clipped": true},
"pose": {"yaw": -18.3, "pitch": 15.1, "dir": "center"}
```

取位置的优先级：

1. **`tracking.cx` / `tracking.cy`**（有 `id` 时）
2. 否则 `face.box` 中心：`(x + w/2, y + h/2)`
3. 不要用 `pose.yaw` 当移动量（那是头朝向，人不动转头也会动舵机）
4. `tracking.dx` 是相对画面中心的归一化位置，可作备用，但本方案主用 **帧差**

ESP32 每收到一帧算：

```text
Δx = cx[n] − cx[n−1]
Δy = cy[n] − cy[n−1]
```

`Δx > 0`：脸在画面里向右移 → 舵机向右加角度（方向不对只改 `SERVO_PAN_INVERT`）。  
`Δy` 只给眼睛，不进舵机。

## 4. 控制环（只在 ESP32）

```text
K230 全量 JSON  ~7–10Hz
        ↓ UART1
   解析 face / tracking
        ↓
   同一 id 才算 Δx、Δy
        ↓
   死区 / 限幅 / 低通
        ↓
   舵机角 += k_pan * Δx     （积分位移）
   眼睛偏移 += k_eye * (Δx, Δy)
        ↓
   速度限制后写 MG90S PWM
```

### 4.1 必须丢掉的帧（避免假移动）

出现任一条件，**更新 last_cx，但不转舵机**：

- `face.present == false` 或 `count != 1`
- `tracking.id` 相对上一帧变了（换人）
- `tracking.clipped == true` 且 `|Δx|` 很大（半张脸进出画面）
- `frame_id` 不连续丢太多帧（按 `ts_ms` 间隔 > 300ms 也重置 last）
- 首帧：只建 last，不跟

丢脸：保持当前角 **800ms**；仍无脸则慢速回 90°。  
不在丢脸瞬间猛回中，避免抖动。

### 4.2 建议初值（真机再调）

| 参数 | 初值 | 作用 |
|------|------|------|
| 死区 `Δx` | 画面宽度的 ~1.5%（或 `\|Δx\| < 3px`） | 滤呼吸、检测抖动 |
| 单帧最大 `Δx` | 画面宽度的 15% | 防跳变当移动 |
| 增益 `k_pan` | 约 `45° / (0.5 * 画面宽)` | 脸横移半屏，头大约转 45° |
| 速度 | ≤ 80°/s | MG90S 平滑，减齿轮冲击 |
| 角度盒 | 45°–135° | 机械安全 |
| 眼睛 | `Δ` 映射到 ±12px，另做衰减 | 停住后眼神慢慢回一点中性，头可以不回 |

眼睛可以比舵机“更活”：位移时看过去，停止 1–2s 后眼神略回中；头停在跟完的角度。

### 4.3 不做 PID 回中

不用 `error = −dx` 去把脸锁在画面中心。  
人站在画面左侧不动，头应停住。  
只有脸继续移动，头才继续加角度，直到碰到 ±45° 盒。

## 5. 和 MCP / 对话的关系

自动跟随默认开，优先级低于用户指令：

| 来源 | 行为 |
|------|------|
| K230 位移 | 默认转头、转眼 |
| `self.eye.look` / 闭眼 | 盖住眼睛；闭眼时停跟随 |
| `self.servo.set_angle` | 暂停跟随，停在指定角 |
| `self.servo.set_follow` | 开/关自动跟随 |
| `self.servo.center` | 回 90° 并恢复跟随 |

不注册“按坐标追脸”类 MCP。模型延迟扛不住 10Hz。

## 6. 实现顺序（确认方案后再写/再编）

1. GPIO8 LEDC 50Hz，能手动到 90° / ±30°（确认 CN2 接线和转向）。
2. 解析 JSON，打日志：`id, cx, Δx, clipped, angle`。
3. 按第 4 节积分 `Δx` 驱动舵机；眼睛跟 `Δx/Δy`。
4. 真机：人左右走、站定、出画、换人，再调死区和 `PAN_INVERT`。

K230 协议不用改。UART 仍收全量帧；“增量”只存在 ESP32 的 `cx[n]−cx[n−1]`。
